# API Gateway (fixture)

HTTP entry point for the platform.

- Proxies to **Service commandes**, **Paiements**, and **Base clients**.
- Exposes `/api/v1/gateway` consumed by **Portail client**.

See `application.yml` and `IntegrationHints.java`.
